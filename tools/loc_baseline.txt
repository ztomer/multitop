# Production files already over the 500-line limit when the ratchet was
# written. Shrink-only: `scripts/local-ci.py` fails if one of these grows,
# and says so when one shrinks so the number here can come down. A file not
# listed may not cross 500 at all.
#
# Tests are out of scope on purpose -- a test file is a list of independent
# cases, and splitting one because it crossed a line count moves cases
# between files without making any of them clearer.
#
# path lines
crates/agent/src/docker.rs 501
crates/multitop/src/panel.rs 560
