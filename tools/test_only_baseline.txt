# Functions reachable only from tests, known when the check was introduced.
#
# This file is a ratchet. A name here is a case the check already found and that
# nobody has resolved yet; anything NOT here fails the gate immediately. When one
# is fixed, its line must be deleted -- the check fails on a stale entry too, so
# the list can only shrink.
#
# Each of these is exercised by tests and by no production path. That is worth
# fixing even when the function is harmless, because the failure mode found twice
# in this codebase is not the dead function itself: it is that a *duplicate* of
# its logic is what production calls, the tests guard the dead copy, and the live
# copy drifts unwatched. `rollback::parse_stored_counter` and
# `LockoutState::on_failure` were both exactly that.
#
# Resolve one by wiring it up, or by deleting it and repointing its tests at
# whatever replaced it. If the caller is real but invisible to the tool, put a
# `// reachability: <why>` note above the function instead of a line here.
