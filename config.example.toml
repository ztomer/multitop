# Servers to monitor. Sudo passwords are NOT configured here: each server has
# its own, set in the Configuration screen (`e`) and kept in the OS credential
# store and the encrypted vault. A `sudo_password` key in this file is not
# supported -- it is plaintext on disk, and if one is found at startup it is
# moved into the credential store and deleted from here.

[[servers]]
host = "192.168.0.33"
port = 22
user = ""
# upgrade_cmd = "apt update && apt upgrade -y"

[[servers]]
host = "192.168.0.90"
port = 22
user = ""
# upgrade_cmd = "apt update && apt upgrade -y"

[[servers]]
host = "192.168.0.158"
port = 22
user = ""
# upgrade_cmd = "apt update && apt upgrade -y"

# show_sparklines = false  # Experimental sparklines; toggle with S in Configuration

